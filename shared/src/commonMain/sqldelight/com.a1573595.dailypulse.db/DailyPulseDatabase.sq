CREATE TABLE Article (
    title TEXT NOT NULL,
    desc TEXT,
    date TEXT NOT NULL,
    imageUrl TEXT
);

insertArticle:
INSERT INTO Article(title, desc, data, imageUrl)
VALUES(?, ?, ?, ?);

selectAllArticleList:
SELECT Article.*
FROM Article;

removeAllArticleList:
DELETE FROM Article;
